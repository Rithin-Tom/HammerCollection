<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Responsive Navbar</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap CSS & Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
<style>
        img.rounded-circle {
      width: 40px;
      height: 40px;
    }

    input.form-control {
      background-color: #d1d5db;
      border: none;
    }

    .bg-white a {
      text-decoration: none;
    }

    .bg-white a:hover {
      text-decoration: underline;
    }


    @media (max-width: 768px) {
  .center-card {
    width: 80%;
    height: auto;
    transform: scale(1);
  }
}

</style>
</head>
<body>

<!-- Top Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-black px-3 py-2">
  <div class="container-fluid d-flex justify-content-between align-items-center">

    <!-- Logo and Brand -->
    <div class="d-flex align-items-center">
      <img src="https://i.pinimg.com/736x/fa/6c/7c/fa6c7cf53db2e908f8166567119352df.jpg" alt="Logo" class="rounded-circle me-2">
      <span class="navbar-brand fs-4">Hammer Collection</span>
    </div>

    <!-- Mobile Search Button -->
    <button class="btn btn-outline-light d-lg-none me-2" type="button" data-bs-toggle="modal" data-bs-target="#searchModal">
      <i class="bi bi-search"></i>
    </button>

    <!-- Mobile Icons Dropdown -->
    <div class="dropdown d-lg-none">
      <button class="btn btn-outline-light" type="button" data-bs-toggle="dropdown">
        <i class="bi bi-three-dots-vertical"></i>
      </button>
      <ul class="dropdown-menu dropdown-menu-end">
  <% if (user) { %>
    <li><span class="dropdown-item-text fw-bold">Hi, <%= user.name %></span></li>
    <li><a class="dropdown-item" href="/profile"><i class="bi bi-person me-2"></i>Profile</a></li>
    <li><a class="dropdown-item" href="#"><i class="bi bi-heart me-2"></i>Wishlist</a></li>
    <li><a class="dropdown-item" href="#"><i class="bi bi-cart me-2"></i>Cart</a></li>
    <li><a class="dropdown-item text-danger" href="/logout"><i class="bi bi-box-arrow-right me-2"></i>Logout</a></li>
  <% } else { %>
    <li><a class="dropdown-item" href="/login"><i class="bi bi-box-arrow-in-right me-2"></i>Login</a></li>
    <li><a class="dropdown-item" href="/signup"><i class="bi bi-pencil-square me-2"></i>Sign Up</a></li>
  <% } %>
</ul>


    </div>

    <!-- Search Bar (Desktop Only) -->
    <form class="d-none d-lg-flex mx-auto w-50">
      <input class="form-control rounded-pill px-4" type="search" placeholder="Search for Products" aria-label="Search">
    </form>

    <!-- Desktop Icons -->
   <div class="d-none d-lg-flex align-items-center gap-3">
  <% if (user) { %>
    <span class="text-white">Hi, <%= user.name %></span>
    <a href="/logout" class="btn btn-outline-light btn-sm">Logout</a>
    <a href="/profile"  class="bi bi-person text-white fs-5"></a>
    <i class="bi bi-heart text-white fs-5"></i>
    <i class="bi bi-cart text-white fs-5"></i>
  <% } else { %>
    <a href="/login" class="btn btn-outline-light btn-sm">Login</a>
    <a href="/signup" class="btn btn-warning btn-sm">Sign Up</a>
  <% } %>
</div>
</nav>

<!-- Bottom Navbar -->
<nav class="navbar navbar-expand-lg bg-white">
  <div class="container-fluid">
    <button class="navbar-toggler mx-auto my-2" type="button" data-bs-toggle="collapse" data-bs-target="#bottomNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-center" id="bottomNav">
      <a href="/" class="mx-3 fw-semibold text-dark">Home</a>
      <a href="/shop" class="mx-3 fw-semibold text-dark">Products</a>
      <a href="/" class="mx-3 fw-semibold text-dark">About Us</a>
      <a href="/" class="mx-3 fw-semibold text-dark">Contact</a>
    </div>
  </div>
</nav>

<!-- Search Modal (Mobile) -->
<div class="modal fade" id="searchModal" tabindex="-1" aria-labelledby="searchModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content bg-light">
      <div class="modal-header">
        <h5 class="modal-title">Search</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <input class="form-control rounded-pill px-4" type="search" placeholder="Search for Products">
      </div>
    </div>
  </div>
</div>

